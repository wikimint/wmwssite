remote_theme: jekyll/minima
markdown: kramdown

# Site settings
title: wmWebStack - HTTP Server Stack for Windows - Apache, PHP & MySQL
description: >- # this means to ignore newlines until "baseurl:"
  wmWebStack is a lightweight, installable HTTP server for Windows with all required server stacks. Easy live publishing in one click.
url: "https://webstack.wikimint.com" # the base hostname & protocol for your site
baseurl: "" # the subpath of your site, e.g. /blog

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
