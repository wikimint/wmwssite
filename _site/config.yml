remote_theme: jekyll/minima
markdown: kramdown

# Site settings
title: wmWbStack - Apache, PHP, MySQL, live publishing server stack for Windows
description: >- # this means to ignore newlines until "baseurl:"
  wmWebStack is a lightweight, HTTP server stack for Windows with Apache, PHP & MySQL. Free and easy setup for developers.
url: "https://webstack.wikimint.com" # the base hostname & protocol for your site
baseurl: "" # the subpath of your site, e.g. /blog

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
